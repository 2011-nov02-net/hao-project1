@model IEnumerable<StoreApplication.WebApp.ViewModels.StoreViewModel>

@{
    ViewData["Title"] = "Index";
}

@{
    Layout = "_EmptyLayout";

}

<h1>Select A Location</h1>

<form asp-controller="Store" asp-action="Index" method="get">
    <p>
        Zip Code: <input type="text" name="zipCode">        
        <input type="submit" value="Search" />
    </p>

</form>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Storeloc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Storephone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zipcode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Storeloc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Storephone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zipcode)
            </td>
            <td>
                @Html.ActionLink("Select", "Select", new { storeLoc = item.Storeloc })

            </td>
        </tr>
        }
    </tbody>

</table>


@*


    <h1>Select A Location</h1>
    <form method="post" asp-action="Index" asp-controller="Store">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label asp-for="Locations" class="control-label"></label>
            <select asp-for="Locations" class="form-control" asp-items="ViewBag.Locations">
                <option value=""> Please select a location to start </option>
            </select>
            <span asp-validation-for="Locations" class="text-danger"></span>
        </div>
        <div class="form-group">
            <input type="submit" value="Select" class="btn btn-primary" />
        </div>
    </form>*@

